{{- if .Values.ingress.enabled }}
apiVersion: {{ include "platformDisks.ingress.apiVersion" . }}
kind: Ingress
metadata:
  name: {{ include "platformDisks.fullname" . }}
  labels: {{ include "platformDisks.labels.standard" . | nindent 4 }}
    service: platform-disks
  annotations:
    kubernetes.io/ingress.class: traefik
    traefik.frontend.rule.type: PathPrefix
{{- if .Values.ingress.extraAnnotations }}
{{ toYaml .Values.ingress.extraAnnotations | indent 4 }}
{{- end }}
spec:
  rules:
  {{- range .Values.ingress.hosts }}
  - host: {{ . | quote }}
    http:
      paths:
      - path: /api/v1/disk
        backend:
          serviceName: {{ include "platformDisks.fullname" $ }}
          servicePort: http
      - path: /api/docs/v1/disk
        backend:
          serviceName: {{ include "platformDisks.fullname" $ }}
          servicePort: http
  {{- end }}
{{- end }}
